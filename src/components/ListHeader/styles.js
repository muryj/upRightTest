import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  container: {
    justifyContent: 'center',
    alignItems: 'center',
    marginVertical: 10,
  },
  headerImage: {
    resizeMode: 'contain',
    height: 50,
    width: 50,
  },
});
